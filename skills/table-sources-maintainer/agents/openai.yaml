interface:
  display_name: "Table Sources Maintainer"
  short_description: "Reconcile manuscript artifacts with source maps"
  default_prompt: "Use $table-sources-maintainer to rebuild the active manuscript artifact inventory, reconcile it bidirectionally with TABLE_SOURCES.md, and verify source paths, owners, and open issues."
