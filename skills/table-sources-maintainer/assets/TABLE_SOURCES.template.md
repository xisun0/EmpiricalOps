---
managed_by: table-sources-maintainer
maintainer_commit:
protected_fields:
  - owner
  - related open issues
---
# Table and Figure Sources
For quick reference, this file shows what figures and tables are in the manuscript, where they come from, who owns them, and which issues remain open.

Updated: **`<date>`**

Draft: [`<path/to/manuscript>.tex`](<remote-blob-url-for-manuscript-tex-at-full-hash>)
at writing commit `<short_hash>`. This draft contains both main and appendix figures/tables.

<!-- If main and appendix artifacts come from separate files, replace the
single manuscript source above with:
- Main source: [`<path/to/main>.tex`](<remote-blob-url-for-main-tex-at-full-hash>).
- Appendix source: [`<path/to/appendix>.tex`](<remote-blob-url-for-appendix-tex-at-full-hash>).
-->

## Main Figures and Tables

| No. | LaTeX label | draft asset | repo source | owner | related open issues |
|---|---|---|---|---|---|
| Figure 1 | `fig:<label>` | `<figure_file.png>` | `<path/to/generator_script>` | `<owner>` | `-` |
| Table 1 | `tab:<label>` | `<table_fragment.tex>` | `<path/to/generator_script>` | `<owner>` | `[#<issue>](<issue-url>)` |
| Table 2 Panel A | `tab:<wrapper>` (`tab:<panel_a>`) | `<panel_a_table_fragment.tex>` | `<path/to/source_panel_a.tex>`, generated by `<path/to/generator_script>` | `<owner>` | `-` |
| Table 2 Panel B | `tab:<wrapper>` (`tab:<panel_b>`) | `<panel_b_table_fragment.tex>` | same filename in `<path/to/code_dir>/`; generator TBD | `<owner>` | `-` |
| Table 3 | `tab:<label>` | inlined in manuscript source | manual table in manuscript; no separate source file | `TBD` | `-` |

## Appendix Figures and Tables

| No. | LaTeX label | draft asset | repo source | owner | related open issues |
|---|---|---|---|---|---|
| Appendix Figure A1 | `fig:<label>` | `<appendix_figure_file.png>` | `<path/to/generator_script>` | `<owner>` | `-` |
| Appendix Table A1 | `tab:<label>` | `<appendix_table_fragment.tex>` | `<path/to/source_table.tex>` | `<owner>` | `-` |

## To Be Confirmed

- `<artifact>`: `<specific uncertainty or follow-up that needs human review>`.

## Notes

- Main and appendix numbering follows the manuscript source listed above.
- `TBD` means the current repo does not make owner, source, or generator
  unambiguous.
- When `repo source` lists only a generator, the code-side source artifact has
  the same filename as the draft asset unless noted otherwise.
- `-` means no clearly related open issue is known.
